# Web (Next.js)

Railo Cloud dashboard built with Next.js. It reads data from the API service.

## Local dev

Set `NEXT_PUBLIC_API_BASE_URL` to point to the API (default: `http://localhost:8000`).

Pages:

- `/runs` and `/runs/[id]`
- `/repos` (mode + enable toggles)
- `/login` (OAuth link)
